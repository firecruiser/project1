A c++ code that controls a dream cheeky missile launcher and uses a kinect to gather imaging data.
The launcher will target all objects of a user defineable color.

To build -

requires:
Qt5
OpenCv
Microsoft Windows SDK
Kinect for Windows SDK
CvBlobsLib
HIDApi
